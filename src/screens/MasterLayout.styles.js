import { makeStyles } from '@material-ui/core'

export default makeStyles(() => ({
  root: {
    width: '100%',
    minHeight: '100vh',
    display: 'flex',
    flexDirection: 'row',
  },
  // hexInput: {
  //   justifyContent: 'center',
  //   alignItems: 'center',
  // },
}))
